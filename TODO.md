# MdOnline 优化待办

## 编辑器样式优化

### 1. 标题下方横线
- **方案**：A — 细线分隔
- **样式**：`1px solid` 浅灰色，自动适配深色/亮色模式
- **适用范围**：全部标题（h1-h6），h1/h2 稍深，h3-h6 更浅
- **宽度**：全宽（与内容区域等宽）
- **状态**：✅ 已实现

## 编辑器交互优化

### 2. $$ 输入自动识别公式块
- **交互方式**：用户输入 `$$` 后按回车，自动转换为公式块（非即时转换）
- **前提**：代码块内不触发
- **状态**：✅ 已实现

## 表格改造

### 3. 表格行列交互系统

#### 3.1 锚点区域定义

表格存在两套锚点区域，共用同一套 DOM 位置，但触发不同交互：

| 区域 | 位置 | 视觉表现 | 交互方式 |
|------|------|----------|----------|
| 间隙锚点 | 两列之间（顶部）、两行之间（左侧）的间隙区域 | 未悬浮：极淡小圆点（4px，surface-300 色）；悬浮：蓝色圆形 "+" 图标 | 悬浮触发插入 |
| 选择格子 | 每列最上方的格子、每行最左侧的格子 | 点击后该行/列整体高亮（accent 色半透明） | 点击触发选中 + 工具栏 |

#### 3.2 插入行 / 插入列（悬浮触发）

**触发条件**：鼠标悬浮在间隙锚点区域

**交互流程**：
1. 鼠标进入间隙锚点区域 → 显示蓝色圆形 "+" 按钮
2. "+" 按钮旁显示气泡提示，内容为「插入列」或「插入行」
3. 鼠标离开间隙锚点区域 → "+" 按钮和气泡消失
4. 点击 "+" 按钮 → 在当前间隙位置插入新行/新列

**插入位置规则**：
- **列**：鼠标悬浮在第 N 列和第 N+1 列之间，点击 "+" 后，在第 N 列之后插入新列（即新列成为第 N+1 列，原 N+1 列及之后的列顺延）
- **行**：鼠标悬浮在第 N 行和第 N+1 行之间，点击 "+" 后，在第 N 行之后插入新行（即新行成为第 N+1 行，原 N+1 行及之后的行顺延）

#### 3.3 删除行 / 删除列（点击选中后触发）

**触发条件**：点击选择格子（行首/列首的格子）

**交互流程**：
1. 点击某行最左侧的选择格子 → 该行整体高亮（accent 色半透明背景），同时在该行附近弹出悬浮工具栏
2. 点击某列最上方的选择格子 → 该列整体高亮（accent 色半透明背景），同时在该列附近弹出悬浮工具栏
3. 工具栏中显示删除图标按钮，点击后删除选中的行/列
4. 点击编辑器其他任意区域 → 取消选中态，工具栏消失

**删除约束**：
- 当表格仅剩最后 1 行时，行删除按钮置灰（`opacity: 0.4; cursor: not-allowed`），不可点击
- 当表格仅剩最后 1 列时，列删除按钮置灰，不可点击
- 表头行（Markdown 表格第一行）与数据行统一对待，不作特殊区分

#### 3.4 工具栏规格

- **内容**：仅删除按钮（后续可扩展复制、剪切、背景色等）
- **定位**：选中行 → 工具栏出现在该行左侧；选中列 → 工具栏出现在该列顶部
- **样式**：与编辑器整体风格一致，圆角、阴影、适配深色/亮色模式
- **行为**：点击删除后工具栏自动消失，选中态自动取消

#### 3.5 状态
📋 待实现
